version: "3.8"

services:
  portfolio:
    container_name: portfolio
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3111:3000"
    restart: always
    networks:
      - proxy
  
networks:
  proxy:
    external: true
    name: traefik_proxy