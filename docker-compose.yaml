version: "3.8"

services:
  portfolio:
    container_name: portfolio
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3111:3000"
    restart: always
    volumes:
      - ./:/app
      - /app/node_modules
